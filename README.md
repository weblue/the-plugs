# Nader's plugin repository

Gradle commands:

```
./gradlew clean build copyDeps publishToMavenLocal -PreleaseToExternalmanager=spacespam -x tests -x checkStyleMain -x checkStyleTest
```

add includes to settings.gradle.kts